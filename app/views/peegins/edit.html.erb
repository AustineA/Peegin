<% @meta_title="Peegin - Edit Your Info" %>
<% @meta_description="Peegin English Diction" %>

<section class="container all-peegin">

  <%= render 'layouts/side_bar' %>

  <div class="col-sm-8">

          <%= form_for @peegin do |f| %>
          <% if @peegin.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@peegin.errors.count, "error") %> prohibited this peegin from being saved:</h2>

              <ul>
              <% @peegin.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>

            <%= f.text_field :title, :placeholder => "Word here" %> <br>
            <%= f.text_area :meaning, :placeholder => "Definition" %> <br>
            <%= f.text_field :example, :placeholder => "Exmaple" %> <br>
            <%= f.submit "Update", class: "btn-submit btn btn-primary" %>
          <% end %>

  </div>

</section>
